# MLOps Platform — Python Dependencies
# For running Katib experiments and the ML training pipeline from Code-Server.

# Katib SDK and Kubernetes client
kubeflow-katib>=0.15.0
kubernetes>=28.0.0

# student_lab library dependencies (storage, training, evaluation)
boto3>=1.34.0
pandas>=2.2.0
scikit-learn>=1.5.0
joblib>=1.4.0
pyyaml>=6.0.0
